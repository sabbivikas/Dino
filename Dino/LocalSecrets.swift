import Foundation

// Local-only secrets. After cloning, paste your real keys here and run:
//   git update-index --skip-worktree Dino/LocalSecrets.swift
// to prevent your local edits from being tracked.
enum LocalSecrets {
    static let openAIKey = ""
}
