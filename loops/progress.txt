LOOP 1 — the atmosphere layer · branch feature/dino-shaders · max 25 iters
===========================================================================
SETUP (2026-07-11): main clean @ 804467e (build 9 untouched, 8× CURRENT_PROJECT_VERSION=9).
Branches feature/dino-shaders + feature/mood-refresh created off main. loops/ scaffolded.

STEP ZERO: cloned github.com/twostraws/Inferno @ depth 1 to /tmp/Inferno.
LICENSE VERIFIED: MIT, Copyright (c) 2023 Paul Hudson and other authors.
Attribution recorded here and in Dino/Shaders/DinoShaders.metal header.
Studied: Water.metal (UV distortion, sin/cos with fmod-wrapped phase),
WhiteNoise/RainbowNoise.metal (dot-product sine hash → fract), 
AnimatedGradientFill.metal (angle-based cycling). Techniques adapted, every
parameter retuned for dino (gentle/slow/warm); no package dependency added.
Secondary ref noted for scroll effects only: eleev/swiftui-new-metal-shaders (unused so far).

---------------------------------------------------------------------------

=== ITER 1 — GradientSeed + shader foundation (2026-07-11) ===
- Appended atmosphere section to existing Dino/Shaders/DinoShaders.metal (kept June ambient shaders):
  dinoHash (time-free — paper never boils), dinoPaperGrain (~0.035), dinoBreathingWater (speed 0.35 vs
  Inferno 3.0, strength swells 0.35+0.65*breath, phase clock external/shared), dinoCausticShimmer (<=6%),
  dinoWeather (rain 1-in-4 columns a0.10 dusk navy / snow warm motes / fog bands a0.05). MIT attribution
  in section header.
- GradientSeed.swift + GradientSeedTests: 6/6 pass. Full DinoTests: PASSED. Debug build: 0 errors.
- Screenshot iter1/baseline-home.png (what's-new sheet — new build re-triggered it; app renders warm and clean).
- RUBRIC (baseline, no visual delta yet — scores reflect unchanged app): tide 4 · storybook 5 · readability 5 · calming 4.
- TODO 1 (GradientSeed) CHECKED — deterministic palette + tests green.

=== ITER 2 — breathing water (2026-07-11) ===
- BreathingCircle outer glow gets BreathingWaterGlow: ViewModifier + Animatable whose animatableData is the
  normalized breath (0..1). It rides the SAME withAnimation the VM uses for circleScale — the transition that
  also fires BreathingHaptics.play — so water, circle, and tide share one clock, zero drift.
- distortionEffect dinoBreathingWater (strength 5, maxSampleOffset 12) + colorEffect dinoCausticShimmer,
  wrapped in TimelineView(.animation) via visualEffect. Reduce Motion → no shader, static soft glow
  (opacity 0.9+0.1*breath).
- Debug arg -breathQARun added (BreathingView.onAppear, DEBUG only) to auto-start a session for screenshots.
- Sim prefs gotcha: what's-new sheet resurfaced; simctl defaults write hits global domain — wrote container
  plist domain path instead (dino.lastSeenWhatsNewVersion=2.0).
- Full DinoTests PASSED, debug build 0 errors. Shots iter2/water-a.png (exhale: glow has soft pebble-like
  water wobble, clearly breathing), water-b.png (rest: settles near-circular, calm).
- RUBRIC: tide 4 · storybook 4 · readability 5 (label/countdown untouched by distortion) · calming 4 → GREEN.
- TODO 2 (breathing water) CHECKED.

=== ITER 3 — paper grain shader (2026-07-11) ===
- ResourcesView.PaperGrain reimplemented: raster line-scan Canvas → Color(#FFFDF6).colorEffect(dinoPaperGrain 0.05).
  Static by construction (position hash, no time) — paper never boils.
- ComfortSlipView background gains the same colorEffect on its fill (before stroke/shadow so shadow stays clean).
- Debug arg -resourcesQA added in HomeView (DEBUG) → viewModel.showResources.
- Full DinoTests PASSED, debug build 0 errors. Shot iter3/resources-grain.png: even fiber on hero + rows,
  reads as real paper stock; tape stripes and hour badges intact; every number crisp.
- RUBRIC: tide 4 · storybook 5 · readability 5 · calming 4 → GREEN.
- TODO 3 (paper grain) CHECKED.
