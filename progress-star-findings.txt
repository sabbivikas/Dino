STAR FINDINGS — experimental demo (branch feature/star-findings)
================================================================
Status: built, verified locally. NOT for merge. NOT deployed.

NOTE ON THIS FILE: the repo's progress.txt is a pre-existing TRACKED ledger for
the comfort-recs / luna-recs arcs. To keep isolation absolute (the top
requirement) and the diff limited to the 3 allowed shipping files + NEW files,
this arc's progress lives in this NEW file instead of editing the tracked one.

THE ARC
-------
An owner-only, flag-gated experimental tab. Tap the star -> it is "sent out" ->
a real browser-agent (AGI vendor) searches the live web for ONE gentle, free
thing to do THIS WEEK in Saint Paul, MN -> a cheap model (gpt-4.1-mini) picks
the single best fit and writes dino's "why" in his lowercase, no-dash voice ->
a reveal card offers one of four outcomes (add to calendar / book it / finish
signing up / warm empty-handed). Past findings + the real comfort recs rest on
an isolated shelf below the star. Four star states: idle, sending (glide off +
fade), away ("the star is out looking"), back (glide-in on a result). Reduce
Motion falls back to opacity, following the copied star class.

ISOLATION RULES (this branch may NEVER merge)
---------------------------------------------
- Everything additive and triple-gated:
  1. client: FindingsFeature.isEnabled == (Auth uid == owner uid), hardcoded.
  2. server: STAR_FINDINGS_UIDS env allowlist, fail-closed, membership only
     (no uid parameter -> firing for another account is structurally impossible).
  3. firestore.rules: starFindings/{uid} + tasks are server-write-only,
     owner-read-own, everyone-else denied.
- Flag OFF -> tab slot 3 is the BYTE-IDENTICAL Gratitude Jar (content, label,
  icon all fall through to the exact prior code).
- Only 3 shipping files edited (additive/flag-gated): MainTabView.swift,
  functions/src/index.ts, firestore.rules. Everything else is NEW files.
- CrisisResources + crisisNet untouched. No .xcstrings entries added. No
  existing test file modified (new test FILES only). No production Firestore
  writes; no deploy. progress.txt (the tracked ledger) left untouched.
- FindingsStar.swift is a VERBATIM copy of Onboarding3D/StarGuide.swift
  (git bf7e3a1), renamed; main's onboarding orb is not touched.

LOCALIZATION = MERGE PREREQUISITE (owner decision on record)
------------------------------------------------------------
English-only demo. All new user-facing copy is plain string literals (no
String(localized:), no new .xcstrings keys). Before this could EVER merge,
every literal in the new Swift files + the "Findings" tab label + the plain
push title/body must move into the string catalog for en/es/ja/ko/vi. (It must
not merge; recorded per the owner's instruction.)

OWNER PII DECISION (on record)
------------------------------
The ONLY personal data that ever reaches the AGI vendor is the owner's OWN
name + email, and ONLY in confirmFinding's booking phase (never search or pick).
Name comes from the client (userName); email from admin.auth().getUser(uid).email.
The booking prompt fills ONLY those two fields and STOPS (fields_blocked ->
handoff) on any form demanding dob / phone / payment / login / captcha.
Vendor: AGI (api.agi.tech) for the agent; OpenAI gpt-4.1-mini for the pick.

AGI API — REAL BEHAVIOR (see scratchpad/findings-QA)
----------------------------------------------------
Real browser-agent sandbox, two planes: control plane api.agi.tech/v1 (session
lifecycle) + per-session agent plane (/user_message + SSE /events + /cancel +
/terminate). start_url is mandatory. The 30-step kill is a live THOUGHT-count
kill on the SSE stream (cancel + terminate + DELETE); the session is ALWAYS
deleted in a finally block. Real runs proved the kill fires at exactly the
threshold and leaks zero sessions.

SURPRISE worth the owner's attention: with the first prompt the search agent
explored productively (found the SPPL library + parks pages) but burned all 30
steps chasing exact per-event permalink URLs and never emitted final JSON --
hitting the step cap with 0 candidates. A convergence tweak to buildSearchPrompt
("be decisive, don't chase permalinks, emit JSON as soon as you have a few")
fixed it: runs 2 & 3 returned 6 real candidates in 5-6 steps / ~90-100s. The
Firecrawl fallback was NOT needed -- the API works; it just needed steering.

VERIFY (all local)
------------------
- functions: npm test -> 154 (137 baseline UNMODIFIED + 17 new pure tests). tsc clean.
- rules: 37 (31 baseline UNMODIFIED in the untouched file + 6 new in a NEW file).
- iOS: Debug TEST BUILD SUCCEEDED; DinoTests -> see scratchpad/findings-QA/build-verdict.md.
- git diff vs main: only the 3 shipping files + new files. No xcstrings, no
  CrisisResources, no existing test modified.

FIX PASS — CLIENT REACHABILITY (two bugs; server was already correct)
---------------------------------------------------------------------
A real production task (fOBoTl257mhN96RRvEjP) completed on the server — status
found, 8 steps, Hatha Yoga Class / SPPL / add_to_calendar — and the client could
not reach it at all. Two independent causes:

BUG 1 — the push had no door. sendFindingPush sent only notification{title,body};
AppDelegate reads userInfo["deepLink"] then userInfo["action"] and otherwise
falls through to home, and ContentView had no "finding" case. FIX: the push now
carries data { deepLink: "dino://finding/{taskId}", taskId }, and ContentView
gained ONE additive case routing it to tab slot 3 + the task's reveal.

BUG 2 — the tab never asked the server. FindingsTabView opened on a hardcoded
idle star and the local shelf only; getFindingTask REQUIRED a taskId, and the
taskId was never persisted until the ~2 minute call returned — so an app killed
mid-search lost the task permanently and had no way to ask about it. FIX:
getFindingTask's taskId is now OPTIONAL (absent → the caller's most recent task,
orderBy createdAt desc limit 1; no task at all → a quiet {taskId:"",
status:"none"} instead of a not-found throw), the client upserts the moment an
id exists and writes a dino.findings.pendingSince marker when a send BEGINS,
and the tab reconciles with the server on appear + on scenePhase .active
(polling every ~10s while a task is still searching and the tab is visible).

THE 4th SHIPPING FILE (owner-authorized): Dino/ContentView.swift, one additive
`case "finding":` in handleDeepLink. SharedDataManager stays untouched — the
published route state lives in the branch-owned FindingsRoute.shared, and only
the already-existing deepLinkTab is set. Shipping files edited on this branch
are now: MainTabView.swift, ContentView.swift, functions/src/index.ts,
firestore.rules.

LIVE READ-ONLY PROOF: the new latest-task query shape run directly against
production (starFindings/{owner uid}/tasks orderBy createdAt desc limit 1)
returns fOBoTl257mhN96RRvEjP with status found, steps 8, the Hatha Yoga finding,
and deep link dino://finding/fOBoTl257mhN96RRvEjP. No production writes.

REDEPLOY REQUIRED (owner runs it): startFindingTask (it calls the changed
sendFindingPush) and getFindingTask (optional taskId). confirmFinding is
unchanged. Until those two are deployed the client fix is inert: the tab's
reconcile call will keep hitting the old taskId-required getFindingTask.

VERIFY (fix pass, all local)
----------------------------
- functions: npm test -> 159 (154 prior UNMODIFIED + 5 new pure tests in the
  NEW file src/findingsReach.test.ts). tsc --noEmit clean.
- iOS: Debug BUILD SUCCEEDED; DinoTests 436 green, unmodified.
- cold-open recovery captured via the DEBUG-only -findingsRecoverQA hook (fake
  terminal task driven through the real applyServerTask path, empty shelf, no
  server call): scratchpad v3-cold-open-recovered.png.


PASS: RAW-REPLY LOG + SOFTENED BOOKABLE BIAS (server-only, no rebuild)
======================================================================
WHY: task oLCcj9OJzWwASFHIIWXp came back outcome=empty with steps=5,
durationMs=105359, preferBookable=TRUE and zero candidates. Not a cap-kill and
not an error, so the agent finished and produced nothing parseable — and we
could not tell "genuinely found nothing" from "replied with prose that parsed
to zero", because the raw reply was never logged. Bias-OFF runs found 6 in 5-6
steps, so the narrowed registration-portal source list was the prime suspect.

1. RAW-REPLY LOGGING. New pure helper summarizeAgentReply(raw, cap) in
   starFindings.ts -> { truncated, looksJson, length }: single-lines the reply
   (so it stays ONE log entry), truncates at AGENT_REPLY_LOG_CAP = 1200, and
   reports the PRE-truncation length plus whether the reply looked like JSON
   ([ or { once ```json fences are stripped, judged exactly as parseCandidates
   sees it). startFindingTask logs "findings_agent_reply" at INFO right after
   parseCandidates, for EVERY run (found and empty) so there is a baseline.
   looksJson is the flag that splits a genuine "[]" from a prose reply. Adds
   ZERO agent steps — it only logs what already came back. No user PII (the
   agent's own text about public listings); the API key / auth headers live in
   agiClient request options and are never in a message body.

2. SOFTENED BOOKABLE BIAS (owner: "I'd rather get candidates with honest
   registration flags than zero candidates"). The preferBookable branch of
   buildSearchPrompt now PREFERS instead of RESTRICTS: registration portals are
   still named FIRST as the preferred starting point, but the agent is told to
   FALL BACK to general gentle listings rather than return nothing, to set
   registrationNeeded HONESTLY per candidate, to prefer (not require) a
   register/sign-up affordance, and that "returning zero candidates when gentle
   free events exist is a failure". The two rules that got runs down to 5-6
   steps are preserved verbatim in the branch: do NOT open each event's detail
   page, and emit the JSON as soon as you have a few candidates. Same step
   budget. The bias-OFF prompt is BYTE-IDENTICAL to HEAD (diffed).

SERVER-ONLY / NO REBUILD: ZERO .swift files in this commit — only
functions/src/{starFindings.ts,index.ts}, two test files and this file. The
client contract is untouched: request stays ["preferBookable": Bool], the
callable response stays { taskId, status, steps, outcome, finding } and the
finding keeps title/when/venue/why/url/registrationNeeded/outcome/startISO/
endISO/dateConfidence. The already-shipped b103 client picks this up as-is.

VERIFY (this pass, all local)
-----------------------------
- functions: npm test -> 182 green (177 baseline + 5 new summarizeAgentReply
  tests); tsc --noEmit clean. The existing preferBookable prompt-branch test
  was extended (not weakened) to also assert the fallback instruction, honest
  flags, "empty is a failure", source-list-before-fallback ordering, and the
  do-NOT-open-each-detail-page rule in BOTH branches.
- iOS: not built and not touched — nothing to build.

REDEPLOY REQUIRED (owner runs it): startFindingTask only. No client work.


PASS: ONE-SHOT JSON RETRY + HARD OUTPUT CONTRACT + CLINICAL EXCLUSION
=====================================================================
(server-only, no rebuild)

WHY: the raw-reply log from the pass above did its job on the very next empty
run. Task opPrIgf6Akl3TpIrXdjD: steps=4 of 30, preferBookable=TRUE,
candidates=0, looksJson=FALSE, replyLength=2820, no error, no cap-kill. The
logged reply proves the agent SUCCEEDED at searching — it had six gentle free
Saint Paul events with dates, times and venues (Garden Storytime, Community
Health Services, Memoir Writing, Talk with a Mental Health Professional, Open
Lab, Playdate) — then NARRATED them in prose, said "Let me compile the results
now", and the session ended before the JSON array was ever emitted. Our loop
took that narration as the final reply, parsed 0, and reported empty. A pure
OUTPUT-FORMAT failure with 26 steps of budget unused. (The same log also shows
the second problem: two of the six were clinical care services.)

1. ONE-SHOT JSON RETRY (the core fix). New pure decision in starFindings.ts:
   shouldRetryForJson({ candidates, looksJson, stepsUsed, maxSteps,
   alreadyRetried }) -> boolean. Retries ONLY when nothing parsed AND the reply
   did NOT look like JSON (a genuine "[]" is an honest empty and is left alone)
   AND we have not already retried AND stepsUsed <= maxSteps -
   JSON_RETRY_STEP_MARGIN (5), i.e. 25 of 30 — so the retry can never be what
   pushes a run into the kill. Fails closed on NaN/negative step tallies.
   The follow-up itself (JSON_RETRY_MESSAGE) is sent to the SAME session: "STOP
   ... reply with ONLY the JSON array of the candidates you just listed", with
   the candidate schema restated and "If you truly have none, reply with []".
   Same session matters — the events are still in the agent's context, so this
   is a FORMATTING turn, not a second search.
   agiClient.ts gained the mechanism, because the session is terminated +
   DELETEd in the finally block and a fresh session would have no memory of the
   search: RunParams.followUp?(ctx) is called AT MOST ONCE, only after a reply
   settles, and NEVER after a kill or an error. Its THOUGHTs land on the SAME
   cumulative tally, so the 30-step kill still fires exactly where it did; the
   vendor is handed only the REMAINING steps as its own max_steps, and no
   start_url (it continues, it does not re-navigate). Deliberately NO event
   de-duplication: if the vendor ever replayed events we would over-count, which
   errs toward killing sooner — the safe direction for a per-step bill. A
   follow-up POST that fails degrades to the first attempt's result (RunResult
   .retryError) instead of turning a finished run into a failed one. The task
   doc still stores the TOTAL steps (initial + retry), so cost telemetry is
   accurate.
   LOGGING: "findings_agent_reply" keeps every existing field and gains
   retried:true|false; it now always describes the FIRST reply, so the prose
   that triggered a retry is never overwritten. When retried, a second entry
   "findings_agent_reply_retry" carries retryCandidates, retryLooksJson,
   retryReplyLength, retryTruncated, stepsBeforeRetry, stepsTotal, killReason,
   retryError and retryReply. An empty run stays diagnosable in one look.

2. HARD JSON-ONLY OUTPUT CONTRACT. buildSearchPrompt now ends (recency: it is
   the LAST thing the agent reads, in BOTH bias branches) with an OUTPUT
   CONTRACT block: the FINAL message must be ONLY the JSON array; "Do not
   describe what you found in words. Your entire final message must start with
   [ and end with ]"; no prose, summary, fence or commentary, and never a "let
   me compile the results now" style narration — listing the events in words
   instead of emitting the JSON is a FAILED run even when the events are
   perfect; a true empty is exactly "[]". The existing performance rules (do NOT
   open each event's detail page, emit the JSON as soon as you have a few
   candidates) and the softened bookable-bias semantics are untouched.

3. CLINICAL / THERAPEUTIC / MEDICAL EXCLUSION (owner: "Talk with a Mental
   Health Professional" surfaced twice). Findings now exclude care delivery the
   same way the comfort recs do. MIRRORED from the recs system prompt in
   runComfortRecGeneration ("only inherently gentle content: nothing graphic,
   violent, frightening, grief centered, or otherwise distressing. never
   clinical or academic works, and never books or films about mental illness,
   therapy, self help, sleep science, or psychological distress; comfort means
   escape and warmth, not a mirror of what they are feeling"), adapted from
   media to events, and noted as such in a code comment. The search prompt gains
   a "NOT CARE SERVICES — dino brings small gentle outings, never care delivery"
   block (both branches) excluding therapy/counseling sessions, "talk with a
   mental health professional" style services, psychiatric/psychological
   consultations, support groups, recovery or twelve step meetings, grief and
   caregiver circles, health screenings, vaccination/flu-shot clinics, blood
   drives, medical and dental clinics, health fairs, medical advice or diagnosis
   sessions, and illness/addiction/mental-illness centered talks — while
   explicitly KEEPING storytime, gardens and nature walks, crafts and open
   studios, music, writing and creative classes, library programs, museum hours
   and quiet community gatherings. buildPickPrompt repeats it as a SAFETY NET in
   dino's lowercase voice, because the pick is the last gate before a finding
   reaches someone's phone.

SERVER-ONLY / NO REBUILD: ZERO .swift files in this commit — only
functions/src/{starFindings.ts,index.ts,agiClient.ts}, the NEW test file
functions/src/findingsJsonRetry.test.ts, and this file. The client contract is
untouched: request stays ["preferBookable": Bool], the callable response stays
{ taskId, status, steps, outcome, finding } and the finding keeps
title/when/venue/why/url/registrationNeeded/outcome/startISO/endISO/
dateConfidence. The already-shipped b103 client picks this up as-is.

VERIFY (this pass, all local)
-----------------------------
- functions: npm test -> 201 green (182 baseline, ALL unmodified, + 19 new
  tests in the NEW file src/findingsJsonRetry.test.ts); tsc --noEmit clean.
  The new tests cover the shouldRetryForJson matrix (retries the incident;
  never twice; never on parsed candidates; never on a genuine []; the 25-of-30
  margin boundary; fail-closed on junk), the retry message, the output contract
  in both branches AND its position at the end, the clinical exclusion in both
  branches AND in the pick prompt, and the runAgentTask follow-up: same session
  reused (one create/terminate/delete), no start_url, remaining-budget
  max_steps, cumulative steps, the kill still firing at exactly 30 across both
  turns, no follow-up after a kill or an ERROR, and a failed follow-up POST
  degrading to the first result.
- iOS: not built and not touched — nothing to build.

REDEPLOY REQUIRED (owner runs it): startFindingTask only. No client work.


REDESIGN: FREE-FLOATING STAR IN DEEP SPACE + TOLAN CARD + TRADING-CARD REVEAL
============================================================================
A full visual redesign of the findings screen. NOT for merge. NOT deployed.

THE SCREEN NOW
- Deep space; the star floats FREE (no shelf, no header, no list). When a
  finding lands, a large Tolan card appears ABOVE the star; the star sits lower
  and smaller beneath it with a clear gap. Nothing else on screen.
- FindingsShelfView.swift DELETED. Past findings now rest on the EXISTING
  profile shelf as keepsakes (see below) — zero shipping-file edits for that.

THE BOX-BUG ROOT CAUSE (owner: "it STILL renders inside a visible box")
- The box was NEVER the SCNView frame. It was the STAR'S THREE GLOW-SHELL PLANES:
  each used material.diffuse = UIColor.black + blendMode .add. Over an OPAQUE
  onboarding scene that black is invisible; over our TRANSPARENT SCNView it
  composites as an OPAQUE BLACK SQUARE — the largest shell drew the biggest box.
  FIX: FindingsStar.buildGlowShells diffuse .black -> .clear (glow only; the
  opaque gold body/face are untouched, still exactly #FFD04D). The glow's
  transparent falloff now stays transparent, so the deep-space backdrop shows
  through and NO edge exists anywhere (verified per-state incl. a zoomed crop).
- FULL-BLEED chosen (owner recommendation): star + Earth share ONE transparent
  SCNView over the whole screen (FindingsStarHostView reworked), positioned in
  scene coords; the star rig drops lower+smaller when a card is up. Also:
  antialiasingMode .none + rendersContinuously true (a transparent MSAA SCNView
  handed back a blank drawable on the sim, and a one-shot render fired at size
  zero before layout). isUserInteractionEnabled false; SwiftUI owns the tap.

BACKGROUND (FindingsSpaceBackdrop reworked)
- Dark-violet base graded WARMER toward the bottom; THREE parallax star layers
  with different sizes/opacities/speeds AND directions (cross-drift = depth);
  TWO nebula washes drifting AGAINST each other; corner vignette. Seeded
  (GradientSeed) — deterministic. Reduce Motion stops all drift.
- EARTH in the SceneKit scene (FindingsEarth): small (~1/3 width), LOW + deep,
  not focal. Procedural equirectangular texture via UIGraphicsImageRenderer
  (soft blue oceans, blobby muted-green continents, cloud wisps, seeded),
  directional key light for the terminator + a dim fill, an additive back-sphere
  atmosphere shell (front faces culled) for the rim glow, slow Y spin stopped
  under Reduce Motion. Its lights use a dedicated categoryBitMask so they never
  re-light the flat gold star.

THE CARD (FindingsCardView, branch-owned SwiftUI)
- ~78% width, radius 26, translucent gradient pale-mint -> green -> warm brown.
  Photo fills the upper portion (AsyncImage w/ graceful fallback to a seeded
  gradient in the same shape — never a broken slot, never an invented photo).
  Two white circular buttons over the photo top-right: share (title+url) and an
  up-chevron that COLLAPSES the card. Bold white single-line title (ellipsis);
  a white pill with the SOURCE DOMAIN (FindingsDomain.source, pure + tested).
- COLLAPSE/RE-EXPAND CHOICE: the up-chevron collapses to the star-only sky plus
  a small unobtrusive "a finding is waiting" chip near the top that re-expands.
- ACTION PATH — OWNER COURSE-CORRECTION APPLIED (veto of the body-tap idea): the
  card body is NOT a tap target. An EXPLICIT per-outcome button sits below the
  source pill (cream fill + dark text, legible on the gradient):
    add_to_calendar -> "add it to my day" (no confirmed time falls back to
      "open the listing" — the honest structured-date rule, never a guessed date)
    book_it         -> "save me a spot"  (name+email booking confirm dialog)
    finish_signup   -> "open the signup page"
  Beneath it a quiet "not this time" dismisses the card to the idle sky, leaving
  the finding UNACTED (no keepsake, no status change) and recoverable via the
  existing reconcile/latest-task path. An already-acted finding shows only the
  quiet acted line ("added to your calendar" / "booked" / "handed off..."). ALL
  honest guards intact: calendarWrittenAt duplicate guard, no second event,
  acted-state persistence, the noConfirmedTime refusal.

REVEAL CHOREOGRAPHY (light first, card second)
- Vertical seed cracks open -> six rays fan out rotating as they expand -> a ring
  pops through them -> eight sparks fly out and fade -> the card materializes on
  a Y-axis flip from nearly edge-on (rotation3DEffect) with a slight overshoot ->
  one diagonal holographic sweep -> a slow warm rim pulse while on screen. ~1.5s.
- ROBUSTNESS NOTE: driven by a SwiftUI .transition (findingsCardReveal flip) that
  always settles at the present state + per-element .animation(value:) light
  burst. An earlier @State/asyncAfter/keyframeAnimator driver got STRANDED
  mid-reveal because SwiftUI re-mounts the card once during the tab-select
  transition (resetting @State / restarting keyframes / Color.clear collapsed
  layout). A transition can never strand invisible — its resting state is present.
  Reduce Motion: light + flip collapse to a plain fade; holo sweep skipped.

HAPTICS (FindingsHaptics, CoreHaptics)
- CHHapticEngine for continuous (GATHER ramp 0.2->0.6 over ~0.4s; LAUNCH .medium
  then continuous decay to zero), UIImpactFeedbackGenerator for discrete
  (tap .light, LANDING settle .soft, BURST .heavy exactly on the ray burst —
  strongest in the app, CARD LOCK .rigid, EMPTY .soft once). AWAY: nothing,
  descent: nothing until settle. Capability-check + engine-start failure ->
  silent no-op (sim included). NEVER UINotificationFeedbackGenerator .error.

PAST FINDINGS -> PROFILE SHELF (FindingsKeepsake, zero shipping edits)
- On the ACTED moment (calendar write succeeded / booked / handoff or signup
  page opened) branch-owned code constructs a RichRec of type "gift" and calls
  RichRecStore.recordKeepsake(rec, kept: true) — both module-internal, callable
  without editing ComfortRecService. Fields mirror ExpeditionGift.asKeepsakeRec
  exactly: title = finding title, creator = venue, why = dino's why line,
  watchLink = the listing url (a shelf tap reopens it). The profile shelf already
  renders type "gift" (dove icon, gold tint). FindingsStore.isShelved/markShelved
  guarantee exactly one keepsake per finding. NO fifth shipping file needed.

SERVER (additive; b103 ignores the new fields)
- imageUrl: candidate schema + buildSearchPrompt capture instruction ("the
  event's OWN photo... never a stock/logo/avatar"), sanitizeImageUrl (https only,
  length cap, plausible image path/host, else null), carried pick -> stored
  finding -> both callable payloads. Client async-loads with the gradient
  fallback. JSON_RETRY_MESSAGE schema updated too.
- tasksRemainingToday: pure helper (5/day cap minus used), added to
  startFindingTask + getFindingTask responses for the idle trip count.
- New pure test file findingsImage.test.ts (imageUrl sanitizer, tasksRemaining,
  parseCandidates imageUrl carry-through incl. snake_case aliases, prompt +
  retry imageUrl assertions).

VERIFY (all local)
- functions: npm test -> 212 (201 baseline UNMODIFIED + 11 new). tsc clean.
- iOS: Debug build SUCCEEDED. DinoTests -> 440 (436 baseline UNMODIFIED + 4 new
  FindingsDomainTests). firestore.rules untouched.
- git diff main --name-only: shipping files are exactly the FOUR (MainTabView,
  ContentView, functions/src/index.ts, firestore.rules) + pbxproj build-number
  lines only. New branch files auto-join via file-system-synchronized groups
  (0 pbxproj entries). FindingsShelfView.swift deleted.
- SCREENSHOTS (scratchpad/findings-QA, driven by extended -findingsStateQA /
  -findingsCardQA args): v5-idle, v5-sending, v5-away, v5-landing, v5-midreveal,
  v5-card, v5-done, v5-unknown, v5-empty, v5-star-edge-zoom — all self-reviewed,
  no box edge in any state.

REDEPLOY REQUIRED (owner runs it): startFindingTask + getFindingTask (the new
additive imageUrl + tasksRemainingToday fields). No confirmFinding change.
