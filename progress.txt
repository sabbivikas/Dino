# 2.1 comfort-recs arc — progress ledger

[2026-07-13 23:18] iteration 1: read GentleRecEngine/GentleRecService/EmotionalWeatherView/functions. FEATURE 1 PLAN POSTED — awaiting owner approval. No code built.
[2026-07-14 12:33] iteration 22: FEATURE 1 COMPLETE + GREEN. Built: functions generateComfortRecs (gpt-4o-mini, batch 3, allowlist keys, 2/uid/day + refund, server validation), ComfortRecService.swift (RichRec, voice, sanitizer, trend buckets, search links, cache+keepsakes store, coordinator w/ unchanged GentleRecEngine gates), RichRecCard.swift, EmotionalWeatherView wiring (rich first, classic fallback), MainTabView QA tab preselect, ComfortRecTests (14 tests). Verified: tsc clean, suite 340/0, sim screenshot f1-richrec5.png (card on mood screen: daypart header no forced moon, title/meta, personal why, flags, feel+length, both music buttons, not tonight). Rubric: personal 5, warm 5, flags 5, cheap 5, voice 5, why-fresh 4 (design enforced; live-API freshness needs deploy). CrisisResources zero-diff. NOTE: functions deploy NOT done (deploys gated on owner confirmation) — client falls back to classic pool until deployed. NEXT: Feature 2 (open-it chooser + remembered default).
[2026-07-14 13:47] iteration 23: FEATURE 2 COMPLETE + GREEN. Open it flow: music asks once (askWhich caption + two equal buttons), choice remembered in dino.recs.musicAppChoice (RecOpenMemory, garbage proof), next time single primary button for remembered app + quiet 'or listen on ...' switch that re remembers. Books/films single destination unchanged. QA: -richRecQA forgets (ask state), -richRecQA2 seeds spotify (remembered state). Verified: suite 343/0 (3 new tests: memory round trip, other() flip, musicLink mapping), screenshots f2-ask.png + f2-remembered.png both correct. Rubric: personal 5, warm 5, flags 5, cheap 5 (zero new network), voice 5. CrisisResources zero-diff. NEXT: Feature 3 (rec keepsakes shelf, jar keepsake grid pattern).
[2026-07-14 13:56] iteration 24: FEATURE 3 COMPLETE + GREEN. The little shelf: RecKeepsakesView (jar keepsake pattern: 2 col pastel LazyVGrid, seeded scatter, cream sheet, close button, empty state), slips show type icon + title + creator + feel + kept date, tap re opens via reopenLink (remembered music app respected, single door otherwise). Entry: paper chip row on mood screen 'your little shelf · n kept' only when keepsakes exist, sheet presents shelf. QA: -richRecQA3 seeds 4 keepsakes + opens shelf. Verified: suite 345/0 (2 new tests: shelfRowLine compose, reopenLink memory), screenshots f3-shelf.png (sheet) + f3-row2.png (entry row). Rubric: personal 5, warm 5, flags 4 (flags live on the rec card; slips carry feel), cheap 5 (zero network), voice 5. CrisisResources zero-diff. ALL THREE FEATURES COMPLETE AND GREEN — RECS_DONE.
[2026-07-14 17:14] country-aware recs: userCountry (ISO region, regex validated) added to allowlist + client payload (Locale.region, nonisolated countryCode helper). System prompt: country informs relevance and availability never the theme, no stereotypes, accessibility (language/subtitles/streamable there), mix local + universal. NO TMDB lookup exists (by design, zero APIs) — availability = prompt rule + regional storefront search links. Verified: tsc clean, suite 346/0, deployed, live tests PH/DE/JP/BR all 200 + contract valid (PH: opm ballads + ang huling el bimbo; JP: murakami + totoro; BR: travessia; DE: universal mix). CrisisResources zero-diff.
[2026-07-14 17:29] attribution accuracy fix: certainty rule alone (4o-mini) killed errors but also killed local resonance; 4.1-mini @ temp 0.8 was WORSE (wrong director for taare zameen par, invented albums); final fix = gpt-4.1-mini + temp 0.5 + canonical works rule (creator defined as artist/author/director never a company, no various artists compilations, famous local beats obscure local). Round 3 live test PH/DE/JP/BR: 12/12 attributions correct (good bye lenin/becker, kafka on the shore/murakami, the way he looks/ribeiro, totoro/miyazaki...). Cost ~$0.0006/call ≈ $0.002/user/month, under the $0.005 cap. Deployed.
[2026-07-14 17:49] tmdb watch providers (films): TMDB_API_TOKEN secret (v3 key detected — helper accepts v3 api_key or v4 bearer), tmdbWatchInfo (search movie by title+year → watch/providers for userCountry, 4s timeouts, never throws — any failure = old behavior). free to them only (flatrate/free/ads); rent only → neutral tmdb watch page ('see where to watch'); no data → apple tv search door. one lookup per batch, cached with the rec (optional Codable fields, back compat). client: 'watch on {provider}' button, tmdb domain validated, foreign links rejected. suite 351/0 (5 new tests), tsc clean, deployed. LIVE: PH totoro → 'watch on netflix' (correct: ghibli is on netflix PH); US amélie → rent only → neutral page, no paywall. CrisisResources zero-diff.
[2026-07-14 17:57] clinical content gap fixed: gentle content rule now bans clinical/academic works and books/films about mental illness, therapy, self help, sleep science, psychological distress ('comfort means escape and warmth, not a mirror of what they are feeling'). Deployed. Stress test 4 rounds (drained/overwhelmed x US/PH/DE/JP, trend=heavy, themes=sleep+self — the exact trigger that produced noonday demon): zero clinical picks, books all escape and warmth (anne of green gables, wind in the willows, little prince, kiki's delivery service). Watch tiers also seen live: netflix PH, amazon arthaus channel DE, neutral page US, search door JP.
[2026-07-14] fleet variety nudge: system prompt line 'vary your picks between listeners: reach widely across artists, eras, and countries instead of defaulting to the same few obvious choices'. Deployed. Same 4 inputs re run: music 3/4 distinct (was 1/4 — for emma forever ago, aeroplane over the sea, in rainbows), book 3/4, film 3/4; remaining repeats are country appropriate (totoro for both PH and JP). Attributions all correct; disney plus DE seen as a new provider tier.

# ── dino agentic layer v1: expedition engine ──
[2026-07-14 20:16] iteration 1: branch created off main @ 43dc294. FULL PLAN POSTED — awaiting owner approval. No code built.
[2026-07-15] APPROVED w/ real rates (luna $1/$6, muse spark $1.25/$4.25 → cohort user ~$0.036/mo; 500 mission cap bounds muse spark at ~$6.30/mo; added 2000/night luna cap ~bounded by cohort). F1 THE WATCHER COMPLETE + GREEN. ModelRouter: one registry, hard rules IN CODE (mission never luna, watching never meta/spark, comfort recs pinned 4.1 mini), model_route cost logging, comfortRecs entry registry only (function untouched). nightlyExpeditionWatch (04:07 utc): cohort query capped 2000/night, strict bucket allowlist, server gates (14 day lastAt, one attempt/night, rec spacing bucket), luna via router (json, 60 tok, temp 0), act needs confidence >= 0.7, all failures quiet. firestore.rules: expeditionSignals strict enum shape owner write; expeditions server write only. Client ExpeditionSignals.swift: pure bucketizers (trend shares ComfortRecTrend), on device eligibility (crisis FIRST absolute, 2 ignore 30 day cooloff, toggle, heavy signal), daily sync from mood .task. Verified: tsc clean, router node tests 6/6, suite 360/0 (9 new ExpeditionTests). DEPLOYED (function + rules) — inert until client ships signals. NEXT: F2 mission — need META MODEL API KEY + BASE URL from owner.
[2026-07-15] F2 CODE COMPLETE (deploy + live test BLOCKED on meta key + base url). mission.ts: validateGift pure (allowlist fields, caps, lowercase, dash strip, 40 word excerpt, url must be one the tools actually visited, gentleness block terms) — 7 node tests. index.ts: runExpeditionMission via router (muse spark, never luna) — tools searchWeb (firecrawl /v1/search) + readPage (/v1/scrape 4000 chars), budget IN THE LOOP: 6 tool steps, 3 reads, 60s wall clock; spend caps as transaction (global 500/mo, per user 2/mo, expeditionSpend/{yyyy-MM}); delivered words via router (4.1 mini, validated, fixed fallback line); gift written to expeditions/{uid} w/ lastAt server timestamp; empty META_API_BASE → missions quietly skip. watcher hookup: pendingNeed → runExpeditionMission, silence on any failure. tsc clean, node tests 13/13, suite unchanged 360/0 (no client edits). AWAITING: meta key → secret, base url → META_API_BASE, then deploy + E2E throwaway test.
[2026-07-15] F3 THE DELIVERY COMPLETE + GREEN (built ahead of F2 deploy — key independent). ExpeditionService: ExpeditionGift model, ExpeditionVoice (all strings voice tested incl settings copy verbatim), ExpeditionParser (re sanitize at the door, needKind allowlist, https only), ExpeditionStore (shown once via lastDeliveredAt — the dove never nags; keep → shelf as type gift w/ url in watchLink; toggle silences), ExpeditionCoordinator.fetchPending (quiet on any failure). ExpeditionCard: paper family, kicker + title + from + quoted excerpt + dino line + keep this / see where it lives / not tonight; opacity only transition (reduce motion safe). Mood screen: gift outranks recs, support outranks both; keep updates shelf count; not tonight → recordIgnore (2 → 30 day cooloff). Profile: little expeditions toggle w/ owner copy. Shelf: gift type (dove icon, gold tint, reopen opens source url). QA: -expeditionQA (public domain dickinson sample). Verified: suite 366/0 (6 new tests), screenshot f3-expedition.png. Rubric: quiet by default 5 (shown once + toggle + cooloff), gentleness 5 (parser + server nets), voice 5, privacy 5 (nothing new leaves device), cost 5 (zero client calls), gift feels found n/a until live E2E. REMAINING FOR EXPEDITIONS_DONE: meta key + base url → F2 deploy → live E2E chain (watcher → mission → delivery) with throwaway users.
[2026-07-15] META API WIRED + F2 DEPLOYED. Key stored as META_MODEL_API_KEY secret v1. Docs (dev.meta.ai/docs): base url https://api.meta.ai/v1, model id muse-spark-1.1 (DOTTED — docs exact string wins over the dashed spec name). Live verified: 200 + valid json. FINDING: muse-spark-1.1 is a REASONING model — default effort starves output (finish=length, all tokens to reasoning); fix = reasoning_effort "low" (snake case; camelCase rejected 400) + mission maxTokens 900→1500 headroom. Router model string updated (one line, as designed), node tests 13/13, tsc clean. META_API_BASE lives in functions/.env (gitignored; value is just the url above — recreate on fresh clone). nightlyExpeditionWatch redeployed with both secrets bound. Cost note: reasoning tokens bill as output → mission ≈ $0.016 to $0.019 each; cohort user worst case ≈ $0.049/mo (slightly over the $0.04 sketch, still bounded: global cap now ≈ $9.50/mo max). NEXT: live E2E chain test (watcher → mission → delivery) with throwaway users → EXPEDITIONS_DONE.
[2026-07-15] LIVE E2E CHAIN PASSED — EXPEDITIONS_DONE. Fixes found by the live run: (1) cohort query needs composite index expeditionSignals(eligible asc, updatedAt asc) — added to firestore.indexes.json, deployed, READY; (2) gpt-5.6-luna rejects max_tokens/temperature (gpt-5 family) — call site now sends max_completion_tokens only, router watching budget 60→200 for reasoning headroom. E2E evidence: throwaway user wrote heavy signal doc THROUGH live rules (200), scheduler triggered watcher, logs show the full router chain (watching→gpt-5.6-luna, mission→muse-spark-1.1@meta, deliveredWords→gpt-4.1-mini), cohort=2 watched=1 acted=1 (owner's sim signal doc correctly gate skipped — sinceLastRec 0to2). REAL GIFT delivered: 'california beavers quietly rebuilding wetlands' (happyeconews.com, needKind rest, whyOneLine 'small engineers at work while you rest awhile', dinoLine 'i found this little calm moment for you, a peaceful pause in the chaos.') — real url from a visited page, all validation passed, read back by the owner user through rules. Earlier run also demonstrated quiet path (acted=0) and swallowed failure = silence. Cleanup: signals doc, expeditions doc, throwaway user all deleted. Card rendering verified in F3 screenshot; parser accepts the live gift shape (needKind allowlist, https, voice rules).
[2026-07-15] FALLBACK ROUTING COMPLETE + LIVE TESTED. Router chains: mission [muse-spark-1.1 → gpt-4.1-mini], watching [luna only — silence is correct], deliveredWords/comfortRecs single. Hard rules asserted on EVERY hop. Gentleness rejections are FINAL (validateGiftWithReason reason gentle = silence, never a fallback); shape/provider/timeout/quota failures = retryable → next hop. One budget grant per mission covers the chain. Telemetry: model_route per attempt + mission_attempt {model, outcome} + mission_fallback marker. Fixes surfaced by the live test: json fence stripping (4.1 mini fences its json; first fallback attempt died on parse), delivered words prompt hardened (never address by name — it called the user 'mary' after the poet in the title). LIVE PROOF (meta base sabotaged to unreachable, then restored + redeployed): logs show mission_attempt muse-spark-1.1 retryable → mission_fallback marker → mission_attempt gpt-4.1-mini gift; real gift delivered end to end via fallback ('10 poems to calm and reassure', marywalker.co.nz, needKind rest). tsc clean, node tests 17/17, both test users + docs fully cleaned, META_API_BASE restored to https://api.meta.ai/v1 and redeployed.

# ── expedition polish arc ──
[2026-07-15] PART 1 TRUSTED-SOURCE-FIRST COMPLETE + DEPLOYED. TRUSTED_SOURCES per needKind (hope/connection = goodnewsnetwork, positive.news, reasonstobecheerful, happyeconews; beauty = poetryfoundation, poets.org, themarginalian; wonder = atlasobscura, apod.nasa.gov, natgeo, bbc; rest [proposed, no seeds given] = themarginalian, emergence, orion, atlasobscura). trustedSourcesFor rotation: unused first, driven by recentSources on the expedition doc (server written, last 6 domains, gift domain moves to front). Mission prompt: site scoped trusted first, ONE wider search only if nothing suitable, owner's small-and-human line verbatim. Budget tightened: steps 6→5, reads 3→2, searches capped 3 (2 trusted + 1 wide) enforced in loop. mission_usage telemetry (try/finally: real prompt/completion tokens per attempt, every exit path). Anti hallucination + validation unchanged. tsc clean, node 20/20 (3 new: per kind coverage, rotation order, unknown kind fallback). Est cost/mission ~$0.011 (from ~$0.016–0.019) — real before/after comes from the live test. NEXT: PART 2 read inside dino (SFSafariViewController reader, shelf drifted handling).
[2026-07-15] PART 2 READ INSIDE DINO COMPLETE + GREEN. GiftReaderView: SFSafariViewController, entersReaderIfAvailable, paper bar tint #FAF6EC, sage controls #5E8A56, close button, presented as sheet (system, reduce motion safe). Card: 'see where it lives' AND the excerpt clipping (contentShape + tap) open the reader in app — never bounced to safari; card stays excerpt only. Shelf: gift keepsakes re open in app after a HEAD aliveness probe (405 counts alive); dead page → 'this one has drifted away' capsule toast (opacity only) and the keepsake remains; non gift keepsakes keep their store doors (UIApplication.open). New voice string driftedAway in allFixedStrings. QA: -giftReaderQA presents the reader directly. Verified: suite 366/0, screenshot p2-reader.png (full dickinson poem readable inside dino). REMAINING: live test 4 missions (rest, wonder, hope, connection) w/ throwaway users + cost before/after → EXPEDITION_POLISH_DONE.
[2026-07-15] LIVE TEST COMPLETE — EXPEDITION_POLISH_DONE. Temp auth gated debugExpeditionMission deployed for controlled needKinds, DELETED after (cloud + code). 4 gifts, all from trusted homes: rest = ten love letters to the earth (emergencemagazine, on retry — first attempt was a GENTLENESS SILENCE, net working); wonder = ottoman bird palaces (atlasobscura); hope = village kept streetlights off 45 days for a bird family (goodnewsnetwork — small human + living world, exactly the brief); connection = pay as you can restaurant bringing strangers together (positive.news). The connection gift arrived VIA FALLBACK (spark invalid output → 4.1 mini) — chain proven in the wild. Measured cost (mission_usage): spark missions $0.0148–$0.0205, fallback mission $0.0146, avg ≈ $0.016/mission vs prior estimate band $0.016–$0.019 — at the floor of the old band; the real win is the hard caps (5 steps, 2 reads, 3 searches) bounding worst case + provenance. One dinoLine used the fixed fallback (delivered words hiccup, by design). All 11 throwaway users + docs deleted. Rubric: gift feels found 5, human and natural favored 5, cost reduced 4 (honest: at the old floor, worst case bounded), gentleness holds 5 (live silence demonstrated), reader opens in app 5 (p2 screenshot), dino voice intact 4 (one fallback line, one 'thought you'd love' slightly chipper).
[2026-07-15] MISSING HEALTH DATA HANDLED — absence is not a signal. sleepBucket/stepsBucket gain explicit 'unknown' (renamed from ambiguous 'none'; branch unshipped so no compat baggage) at all three layers: client bucketizers (nil → unknown, freshness inherent — callers query last night/today only so stale arrives as nil), firestore.rules allowlists, server EXPEDITION_SIGNAL_ALLOW (moved to mission.ts + buildLunaUserPrompt extracted pure for node tests). Luna prompt gains owner line verbatim (unknown = no information, judge from what is known, MORE conservative when knowing less). VERIFIED: rec payload never had sleep/steps keys and nothing assumes them (3); cohort eligibility takes zero health inputs — mood only users enter on mood signals alone (4, new client test); no nagging — expedition layer has no health UI or requestAuthorization, the pre existing mood screen steps invite is the only mention, untouched (5). Tests: node 23/23 (+3: allowlists carry unknown not none, luna payload passes unknown verbatim/fabricates nothing, no fabricated looking values), suite 367/0 (+1 mood only cohort test, bucketizer tests updated). Deployed: rules + nightlyExpeditionWatch.

# ── forward-port of 2.0.1 hotfix ──
[2026-07-15 18:37] PORT: crisis fix (460f5e4) — PL (centrum wsparcia 800 70 2222 24h), TH (dmh 1323 24h), ID (sejiwa 119 ext 8 NO badge per house rule + healing119.id). Directory 27 countries. Baseline hash sha256 631317b4b47c105178a3f962a8ed6d554372eb255a3f277d0e1a5bdc05c7fc98.
[2026-07-15 18:37] PORT: language fix (c7d7c85) — knownRegions en/Base, catalog stripped of unapproved es/ja/ko/vi, AppLanguage helper, six senders on app language, -resourcesQA hook merged into main's debug block. PLUS main-only extension: ComfortRecCoordinator userLocale → AppLanguage.current (userCountry stays region based).

# ── full localization arc (es ja ko vi) ──
[2026-07-15 18:42] branch feature/localization off main @ 758c14c (precondition passed: AppLanguage present, catalog clean 60 keys). NOTE: found stale pre port branch of the same name at 9bf9236 (the original auto localization work, likely source of the 60 unapproved translations) — archived as archive/auto-localization-pre-hotfix, not reused. EXPERIMENT RESULT: xcodebuild does NOT auto extract Text literals into the catalog (0/419; that sync is an Xcode IDE behavior) — extraction will be scripted + verified manually. PHASE 1 next: inventory user facing literals per file, extract in tranches, suite green per tranche.
[2026-07-15 19:26] PHASE 1 TRANCHE 1 COMPLETE: +255 net keys (275 simple Text literals + .localized keys added, 20 non linguistic pruned — punctuation/emoji need no translation). Catalog 60 → 315 keys. ZERO source edits (SwiftUI Text literals are LocalizedStringKey at runtime; extraction = catalog membership only, english rendering mathematically unchanged — no foreign units exist). Build fix: STRING_CATALOG_GENERATE_SYMBOLS YES → NO (was on, zero usages; emoji keys and case variant keys cannot make Swift symbols). Suite 367/0. REMAINING PHASE 1: interpolated Text literals (format keys), 9 voice constant modules (String(localized:) + en pinned test strategy), non Text hardcoded strings (alerts, labels), key screen screenshots.
[2026-07-15 19:48] PHASE 1 TRANCHE 2: 47 interpolated Text literals → 37 new format keys (%@ / %lld inferred by numeric hints; mismatch risk = safe english fallback, flagged in format-keys-report.txt for phase 3 runtime verification). Catalog 315 → 352. Suite 367/0. NEXT: the 9 voice constant modules (String(localized:) + en pinned tests), then non Text hardcoded strings, then screenshots + extraction report.
[2026-07-15 19:52] PHASE 1 TRANCHE 3: 59 voice constants across the 9 modules wrapped in String(localized:) (enum block scoped; EXCLUDED by design: protocol arrays — allowedFlags/Types/Feels/needKinds stay raw for server contract, UserDefaults keys, urls). Catalog +54 → 406 keys. Suite 367/0 — en values identical so voice contract tests pass unchanged. REMAINING: func internal voice literals (kicker/header/rotating line arrays incl SiriReplies/WeeklyDigest/JournalMoments), non Text hardcoded strings, key screen screenshots + extraction complete report.
[2026-07-15 19:55] PHASE 1 TRANCHE 4: 23 strings — SiriVoice spoken line arrays, WeeklyDigest sparse lines, ComfortSlip.kicker daypart composer (4 dayparts + format key 'a small comfort · %@'), ComfortRecVoice.header ternary. Catalog → 429. Suite 367/0 (en identical, siri rotation determinism tests pass unchanged). REMAINING: JournalMoments seedLines (name interpolation format keys), WhatsNewSlide, alerts/nav/confirmationDialog titles, screenshots + extraction report.
[2026-07-15 19:58] PHASE 1 TRANCHE 5 + PHASE 1 COMPLETE: JournalMoments (consent copy + seed line composers as format keys), WhatsNewSlide titles/bodies, non Text SwiftUI surfaces (Button/Label/Toggle/TextField/Section/confirmationDialog/navigationTitle/alert/accessibilityLabel — LocalizedStringKey based, catalog membership only). Catalog FINAL: 487 keys. EXTRACTION REPORT — extracted: 255 simple Text + 37 format keys + 59 voice constants + 23 rotating/daypart + 58 tranche 5 = 432 keys added to the original 60, minus 20 pruned non linguistic. DELIBERATELY SKIPPED: CrisisResources (protected, per country data verbatim), debug/QA strings (#if DEBUG hooks, qaSample), log/print messages, UserDefaults keys, urls, server protocol values (flags/types/feels/needKinds — client display of server enums flagged as phase 3 item via LocalizedStringKey render), analytics event names, deep link hosts. Screenshots p1-en-home/p1-en-mood saved (english unchanged; brightness 205/203 nominal). Suite 367/0 at every tranche. PHASE 2 NEXT: translation w/ per language voice briefs, review sheets, STOP at gate.
[2026-07-15 20:01] PHASE 2 PRE-PASS (catalog hygiene): dropped malformed format keys (regex had cut at nested parens — could never match at runtime), debug/QA leakage (RENDER RETURNED NIL, postcard qa, uid rows), untranslatable fragments; re extracted interpolated literals with balanced paren parsing. Suite green. Translation starts next iteration: es first, voice lines individually, review sheets to LocalizationReview/.
[2026-07-15 20:21] PHASE 2 ES VOICE CHUNK: 157 voice lines translated (tú register, lowercase, no dashes, warmth per brief; back translations included) → LocalizationReview/es-voice.json, NOT in the shipping catalog (gate first). Fixes en route: 3 %lld false positives corrected to %@ (country/weekday matched numeric hints), 7 missing String(localized:) format keys added w/ better heuristic, escape broken keys converted (15), ceremony 'from a dino in %@' wrapped. Catalog 476 keys, suite green. NEXT: es UI chunk (~319 remaining), then ja/ko/vi, review sheets, GATE STOP.
[2026-07-15 20:28] PHASE 2 ES COMPLETE: 477/477 keys — 157 voice (individual attention + back translations), 303 ui, 24 no translate (decoration/attribution/version/debug; listed in no-translate.json). All in LocalizationReview/, NOT the shipping catalog. NEXT: ja, then ko, then vi, then review sheets + GATE STOP.
[2026-07-15 20:30] PHASE 2 JA VOICE CHUNK: 162 voice lines (soft plain/です mix, gentleness by register, no keigo, no clinical terms; back translations included) → LocalizationReview/ja-voice.json. NEXT: ja ui chunk, then ko, vi, review sheets, GATE STOP.
[2026-07-15 20:38] PHASE 2 JA COMPLETE: 477/477 (162 voice + 296 ui + no translate; the 5 seed format keys live in the voice file). NEXT: ko, vi, review sheets, GATE STOP.
[2026-07-15 20:41] PHASE 2 KO VOICE CHUNK: 162 lines (해요체 warmth, 다정함 register, back translations) — full parity with ja voice. NEXT: ko ui, vi, sheets, GATE.
[2026-07-15 20:46] PHASE 2 KO COMPLETE: 477/477 (162 voice + 296 ui + no translate). NEXT: vi, sheets, GATE.
[2026-07-15 20:56] PHASE 2 VI VOICE CHUNK: 162 lines (thân mật register, bạn/mình warmth, lowercase, back translations) — full parity. NEXT: vi ui, then sheets + GATE.
[2026-07-15 21:09] PHASE 2 COMPLETE — TRANSLATION GATE, STOPPED. All four languages 477/477 (162 voice w/ back translations + ~296 ui + 24 no translate each). Review sheets: LocalizationReview/REVIEW-{es,ja,ko,vi}.md — voice lines first for native speaker review. NOTHING in the shipping catalog. Awaiting 'translation gate approved'.
[2026-07-15 21:32] PHASE 3 COMPLETE — LOCALIZATION_DONE (wiring only, NOT shipped, branch unmerged, no train). Injected 1812 translations (453 keys x 4 langs), knownRegions restored, COMPLETENESS SWEEP 0 GAPS (alex bug structurally impossible: every translatable key has all four languages; 24 no translates english by design). LocalizationTests: per language voice contract (bundle exists, voice strings resolve, dash free, lowercase where cased) — suite 369/0 on CLEAN build, app ships en/es/ja/ko/vi lprojs. Live AI: letters in language 4/4 first try; recs es ok but ja/ko/vi came back english → server fix (reinforced language directive in generateComfortRecs: why/length in language, titles original) deployed + retested 3/3 in language. Screenshot matrix: 20 shots (mood/resources/onboarding/ceremony/rec card x es/ja/ko/vi) in scratchpad loc-shots/ — the review material for native speakers, brightness nominal (image read down; speakers verify content). Corrections from review go into the catalog BEFORE any train.

# localization audit arc — english-leak audit + fix (2026-07-15/16)
[2026-07-16] AUDIT: owner reported onboarding letter in english in all 4 languages. root cause class: Text(variable)/ternary/interpolation-outside-Text/system surfaces bypass LocalizedStringKey. real known hole = LetterView.swift typewriter letter (ContentView first launch) + onboarding quotes/options.
[2026-07-16] scan: 903 flagged literals in 95 files; 7 classifier agents traced render paths → ~500 confirmed leaks incl ALL notification bodies (NudgeLibrary 65), in-app notification center (~30), 35 affirmations + 30 widget affirmations, breathing patterns/coach (incl US crisis card copy in BreathingCoach.swift — NOT CrisisResources.swift, untouched), world tab voice, rhythms insights (english weekday names), weekly digest fallbacks, garden postcards, share texts, siri replies, live activities, ALL home-screen widgets (separate target), theme names, errors/empty states/toasts/a11y.
[2026-07-16] 6 fixer agents wrapped every leak (conventions: String(localized:), ternary branch wraps, format-key interpolation, DUAL label/value splits — stored values NEVER change, display via .localized). english byte-identical. all files parse-clean.
[2026-07-16] structural: InfoPlist.xcstrings (11 permission strings ×4), AppShortcuts.xcstrings (9 siri phrases ×4, FLAGGED for native review), DinoLiveActivity/Localizable.xcstrings (128 keys) + InfoPlist.xcstrings, widget target STRING_CATALOG_GENERATE_SYMBOLS=NO, flipped 3d/7d/10d/14d+/min from no-translate.
[2026-07-16] server: deliveredWords locale thread — client sends userLocale in expeditionSignals (rules updated+deployed, optional field, old clients valid), server prompt + localized fallback line per language (functions deployed; dormant until branch ships).
[2026-07-16] extraction ground truth via xcodebuild -exportLocalizations: app catalog 477→1131 keys, widget 128. translated 828 new keys ×4 (8 batches, voice briefs, dash-lint clean). COMPLETENESS SWEEP: 0 gaps across all 5 catalogs. review sheets updated with audit-additions files + extra-attention list.
[2026-07-16] known caveats for owner: analytics now receives localized weekday/helix-formation names on non-english devices; BreakScheduler slot displayTime stays en format (server contract — follow-up needed); crisis keyword nets (BreathingCoach/server) are english-only by design — non-english crisis text won't match, flagged as future work; JournalEntry titles/summaries persist in language-at-creation; ko register mismatch: getLanguageInstruction says 반말, catalog brief used 해요체 — for native reviewers to judge.
[2026-07-16] KOREAN WALK (xcuitest walker, DinoUITests/KoreanWalkUITests): 6 test flows — letter, sign-in, onboarding (14 steps tapped through), main tabs+home sheets (fresh launch per destination), 7 mood QA states, profile sheets by index. ~120 ko screenshots + 38 each es/ja/vi. agent-eyes verification on every shot (WOZCODE image-read down; host Read agents used).
[2026-07-16] walk CAUGHT + FIXED: (1) tab bar labels english (MainTabView Text(variable), missed by scan — single words); (2) THE ALEX-BUG SHAPE: dynamic-only keys (.localized runtime lookups) invisible to extractor were never ADDED to catalog — 174 keys injected (referral/challenge options, rec vocab music/book/film/cozy/hopeful/quiet, 5 content-flag lines, profile row labels); (3) GrowthView stage labels + watering timeline (today/3d/7d/10d/14d+) displayed raw; (4) RecKeepsakesView feel tag third render path. catalogs now: app 1308 + widget 128 + infoplists + appshortcuts = 1458 keys, 0 gaps.
[2026-07-16] walk EXPECTED-english (documented, not bugs): QA fixture content (rec why/length, expedition dinoLine — live server generates in-language, verified earlier); media titles/creators; provider names; system permission dialog chrome (device locale); "감정 dna" (kept, universal abbreviation — native reviewers to confirm).
[2026-07-16] OWNER ITEMS from walk: CrisisResources ko entry shows english labels ("call 109", "danuri helpline (multilingual)", "support in many languages", immediate-danger footer) — PROTECTED FILE, untouched, needs owner-managed per-language labels; ko onboarding step02 first glyph clipped at left edge (WordRevealText layout with ko text); 오늘의 포커스 icon renders as grey noise square (asset issue, language-independent).

# crisis localization arc — gates 1+2 applied (2026-07-16)
[2026-07-16] GATE 1 APPLIED (owner approved): CrisisResources.swift — KR danuri → "다누리콜센터 (danuri)" / "여러 언어로 도움을 받을 수 있어요"; JP details → "無料・秘密厳守、受付時間はさまざま" / "午前10時から午後10時まで"; action verbs + secondaryLabel + fallbackLine + emergencyFooter + 3 international details wrapped String(localized:) (english byte-identical), 11 catalog keys ×4 languages. es/mx/ar already in-language, unchanged. numbers/orgs untouched. NEW PROTECTED-FILE BASELINE HASH: ba2ebd5645e249d0f5148235e6c83de25ebd39b4b653654e60b1a048a763e090 (replaces 631317b4).
[2026-07-16] GATE 2 APPLIED (owner approved w/ conditions): matcher bugs fixed FIRST — server normalizer unicode ([^\p{L}\p{N}]+/gu; old class erased all non-ascii), de-spaced substring class carries ja/ko (client despacedCore generalized to multilingual substrings). four keyword sets shipped client (BreathingCoach.swift) + server (functions/src/crisisNet.ts, extracted pure module) in sync, ALL nets always active. TESTS: DinoTests/CrisisNetTests (7 tests: positive per language incl spaced/despaced ko, no-space ja, accentless es, ascii vi; negatives incl ㅈㅅ, 無理しないでね, tự túc) — suite 377/0; server crisisNet.test.ts — node 42/42. deployed (suggestBreathingSession). keyword lists added to REVIEW-*.md as SAFETY-CRITICAL walk incl named ja question re もう無理.
[2026-07-16] runtime evidence: ko resources screen re-captured post-gate — both KR cards fully korean ("109 전화하기" / "다누리콜센터 (danuri)" / korean disclaimer), zero stray english (ko-resources-after-gate1.png). note: "1577-1366 전화하기" wraps to two lines on the button (cosmetic).
[2026-07-16] VN fast-follow research delivered for owner verification (numbers NOT added to directory).
[2026-07-16] BANKED: persisted in-app notifications keep creation language — store keys not strings, small data-model fix, owner to schedule.

# memory + shelf arc — feature/agent-memory (off main)
[2026-07-16] iteration 1: read prior arcs (recs, expeditions, signals privacy shape, keepsakes store). FULL PLAN POSTED — awaiting owner approval ("plan approved"). No code built. NOTE: branch is off main which is PRE-localization (feature/localization unmerged) — all new strings will carry es/ja/ko/vi translations in the catalog from day one (permanent rule), but the four lprojs only ship once feature/localization merges; merge-order risk flagged in plan.
[2026-07-16] F1 GREEN: OutcomeLedger.swift (enum-only writes, pending-slot action routing, followup sweep rides daily signal sync — no new pipeline). 6 wire points (rec shown/opened/notTonight/ignored, gift shown/opened/kept/notTonight). rules: outcomes/{uid}/entries strict create/update shape + prefs server-write-only, DEPLOYED. suite 372/0. LIVE RULES BATTERY 13/13 (valid rec+gift create, action + followup updates pass; podcast type, free-text field, rec-with-domain, kind flip, off-enum trend, delete, 60-char domain, prefs write, cross-user read all 403). retention: expiresAt +365d (TTL policy to enable in console at ship: collection group 'entries', field expiresAt) + distiller prunes to 200 (F2). test account created via admin API (anon auth disabled), removed after.
[2026-07-16] F2 GREEN: modelRouter 'preferences' task → gpt-5.6-luna (hard rule in code: never meta/muse-spark; router tests extended). preferences.ts pure module (DISTILLER_PROMPT, buildDistillerInput enum lines, validatePrefs clamps to allowlists + user's-own-ledger domains, max 4). nightlyPreferenceDistill (4:23am, cap 50 users/night, 2000 runs/mo via prefsSpend transaction; prunes ledger to 200 on touch; N=8 minimum). collection-group index entries.shownAt deployed. node 49/49. LIVE RUN on 22-entry seeded ledger: learned music-lands/film-ignored/rest-ignored/avoid-atlasobscura/evening — exactly the seeded pattern; needKindsLanding conservatively empty (safe direction). LESSON (live): luna reasoning tokens bill as output — 300 cap truncated json (prefs_invalid); 1000 works (995 completion incl reasoning). COST actuals: 567 in + ~995 out ≈ $0.0066/run → realistic 8-15 runs/user/mo ≈ $0.05-0.10/user/mo — slightly above plan estimate ($0.02-0.07), still trivial; global cap bounds worst case at ~$13/mo. IMPORTANT branch note: functions/src + firestore.rules unified with feature/localization's newer server tree first (this branch was off pre-crisis-arc main; deploying from it would have reverted the multilingual crisis nets + userLocale rules — caught before deploy, rules re-deployed as superset).
[2026-07-16] F3 GREEN: consumers read prefs (null-safe, derived-data). recs: one bias sentence before the language instruction, contract untouched, fleet-variety kept — LIVE BEFORE/AFTER: no-prefs [music,book,film] → prefs(book lands, music ignored) [book,film,music]; batch order = consumption order, so the next rec flips from the ignored type to the landing type while variety survives. missions: keptKinds bias line in missionPrompt, avoidDomains merged into the recentSources rotation (deterministic test: avoided domain rotates to back), giftFatigue in watcher prompt + IN-CODE act threshold 0.7→0.85 at high fatigue (generalizes 2-ignore cooloff; client cooloff untouched). LIVE: control + biased users through the real nightly watcher → two real muse-spark gifts delivered end-to-end through the biased path (luna chose rest for both, so the wonder-domain avoidance shows in the deterministic test rather than on screen). node 51/51. validation/gates unchanged everywhere.
[2026-07-16] F4 GREEN: full-archive shelf. Keepsake gains kept/keptAt/ledgerId (custom decoder migrates old entries as kept=true — continuity); cap 24→200 mirroring the ledger; markKept returns ledgerId for the enum twin. every rec shown AND gift delivered now writes shelf+ledger at display (one event, two faces); ExpeditionStore.keep marks instead of duplicating. UI: brought-line chip (live: "your little shelf · 5 things dino has brought you"), everything·kept filter (default everything), washi-tape mark on kept, unkept plainer + "keep this" (writes lateKept via stored ledgerId), empty state new line, subtitle stays "%lld kept" (kept-count remains visible; deliberate). card color now rides title hash — stable across filters (verifier catch). 6 new strings ×4 languages in catalog (permanent rule) + MemoryShelfLocalizationTests sentinel (reads catalog source via #filePath — works pre-localization-merge) + migration test. QA seed alternates kept/unkept. screenshots verified by agent eyes: grid mixed ✓ filter ✓ late-keep (2→3 kept, button disappears) ✓ chip ✓ — in MemoryShelfQA/. suite 375/0. reduce motion: no new animation anywhere (washi + filter are static).
[2026-07-16] MEMORY_SHELF_DONE — all four features green. UITest infra (scheme testable, pbxproj TEST_TARGET_NAME) ported from feature/localization for the shelf-states test.

# ── overnight autonomous arc — rec delivery (feature/rec-delivery, off integration/convergence) ──
HARD LIMITS: never touch main or push anywhere; no App Store/TestFlight/production deploys; CrisisResources.swift content changes FORBIDDEN (protected-file sha256 must stay ba2ebd5645e249d0f5148235e6c83de25ebd39b4b653654e60b1a048a763e090); never touch /Users/vikas/Dino (hotfix/2-0-1, owner's uncommitted work) or /Users/vikas/Dino-star (parked); all work in /Users/vikas/Dino-conv. owner asleep — no approvals waited on; judgments recorded in this ledger + CONVERGENCE-NOTES.md.

TASK LIST:
[DONE] convergence — integration/convergence built: main + feature/localization (fast-forward, 0 conflicts) + feature/agent-memory (4 conflicts resolved per resolution law). build green, DinoTests 385/385, functions 51/51, crisis hash verified ba2ebd56…, catalogs 1324/128/12/9/1 keys 0 gaps. see CONVERGENCE-NOTES.md.
[DONE] F1 shelf-under-profile — the little shelf moved to a profile row ("your journey", reuses shelfTitle/shelfEmptySub keys — zero new strings); mood screen is pure check-in (shelf chip + rich rec card + comfort slip + expedition card + fetch plumbing removed; generation + ExpeditionSignals sync untouched; support surfaces stay). -richRecQA3 → profile; dead mood QA hooks removed; KoreanWalk test05 updated; F1RecDeliveryUITests added. build green, DinoTests 385/385, 4 QA screenshots + f1-verdict.md in scratchpad rec-delivery-QA/. crisis hash ba2ebd56… intact.
[DONE] F2 delayed-delivery server — recs generate at mood-log time (trigger restored in EmotionalWeatherView; support row still wins) but HOLD: generateComfortRecs stores {recDeliveries/{uid}/deliveries (enum+timestamp metadata: status held|announced|opened|expired, deliverAfter, daypart, tz, attempts) + payloads (the recs, for F4's status-gated reveal)} and returns {held, deliveryId} — generation contract untouched. recDeliverySweep (7-59/10 cron, batch 200, pure policy in recDelivery.ts): deliverAfter = +45..90min random → pushed out of quiet hours 21:30-08:30 USER-LOCAL (tz from new presence/{uid} heartbeat: lastActiveAt serverTimestamp + IANA tz, scene-active + 2min timer; utc floor when absent) → past the 1/day cap (lastAnnouncedDayKey in the announce transaction — idempotent, never twice) → never into an active session (heartbeat <3min → back off 15-30min) → held 72h unlandable → expired. one held delivery at a time (no model-call burn). rules: deliveries/payloads owner-readable ONLY when status != held (no-leak, server-enforced), presence strict 2-field write-only. sendRecAnnouncement log-only stub = the F3 boundary. collection-group index deliveries(status,deliverAfter) added. functions 81/81 (+30), rules 15/15 (+10, emulator), DinoTests 388/388 (+3), crisis hash intact. f2-verdict.md in scratchpad rec-delivery-QA/.
[DONE] F3 announcement (push + live activity) — sendRecAnnouncement is real: FCM send with apns loc-keys ONLY (title-loc-key rec_announcement_title / loc-key rec_announcement_body — content-free by construction, leaf-walk tested; recAnnounce.ts pure + 7 tests, functions 88/88). push infra built from zero: FirebaseMessaging added via SPM, aps-environment entitlement, AppDelegate APNs/FCM plumbing, pushTokens/{uid} owner-write rules (+6 rules tests, emulator needs Java — not runnable here) with the prefs contract: token stored ONLY while system permission + master toggle + signed in; anything else deletes the doc (server-side mute → silent skip). CONSOLE SETUP STILL NEEDED: push capability on the App ID + APNs key in firebase (flagged in f3-verdict — device signing may complain until then). parcel live activity: RecParcelActivityAttributes (6h lifetime) + RecParcelLiveActivity (drawn parcel — cream box, string cross, washi "?" — gently glowing, "dino has something for you", DI compact mini parcel + "what is it? 🎁", widgetURL → dino://rec-reveal/{id}); client-side start on foreground observation of 'announced' (push-to-start needs the same provisioning — closed-app gap flagged honestly), ends on open or 6h sweep; master toggle off also keeps the parcel down. deep link rec-reveal/{deliveryId} → clearly-marked F4 placeholder (existing strings only). strings: exactly the locked 2 push keys (app catalog ×5) + LA line + reused body (widget catalog ×5), ko 해요체 per shipped catalog voice. DinoTests 397/397 (+9). QA caught a real bug (bodyLocKey → locKey — banner body missing on sim until fixed). 4 verified screenshots + f3-verdict.md in scratchpad rec-delivery-QA/. crisis hash ba2ebd56… intact.
[DONE] F4 reveal screen — RESUMED a partial attempt (prior agent died on a credits error; on-disk work was ~complete + coherent). Verified every referenced API compiles, finished verification, cleaned the stray emulator log, committed (3f8b66d). RecRevealView: full-screen fullScreenCover (not a sheet) — dusk world backdrop (app's evening-sky purples + two evening hills) + existing cut-DinoMascot presenting + RecParcelView glowing center; tap → ~0.95s unwrap (four cream paper quarters fold/fly outward, scale/rotate/opacity only, no timers → 60fps) → card blooms 0.92→1 on a spring; swipe-down dismisses; reduce-motion fades straight to card. IMAGE-LED, keyless per type: film→tmdb poster_path (server now validates+stores via posterPathOrNull, no client API) + documented image base; book→open library covers; music→itunes artwork upscaled 600x600; RecArtwork all nil-on-doubt (5s timeout, https-only, ≤10px blank rejected) → paper-only card, NEVER a broken frame/gray box/SF-symbol. OPENED TRANSITION: narrow firestore.rules update (matches tree convention, not a callable) — owner-only announced→opened, affectedKeys⊆{status,openedAt}, openedAt==request.time; create/delete stay false; +6 emulator rules tests. RULE: card revealed=opened (mark opened + end live activity + legacy presentRichRec keepsake/ledger 'shown' writes + cache the 2 companions); dismissed-while-wrapped/mid-unwrap stays 'announced' for F5's shelf catch (isFirstReveal gate → no double-write on stale re-tap). 'open it' = shelf's reopenLink open flow (remembered music app) + ledger 'opened'; 'share' = share sheet (title · creator + reopen link). ONE new string: "open it" ×4 (es ábrelo / ja あけてみて / ko 열어봐요 / vi mở ra xem); "share" reused; zero existing-string changes. SUITES: DinoTests 418/418 (397+21 RecRevealTests), functions 90/90 (88+2 posterPathOrNull), rules 27/27 (21+6, emulator w/ homebrew openjdk 26), Debug build green (placeholder deletion breaks nothing). 6 required screenshots + 2 bonus, all agent-eye-verified real UI (f4-card-film has the REAL tmdb totoro poster loaded on sim; f4-card-paperonly is a clean no-image fallback). crisis hash ba2ebd56… intact; /Users/vikas/Dino + /Users/vikas/Dino-star untouched; nothing pushed/deployed. f4-verdict.md (prior agent's, independently reconfirmed) in scratchpad rec-delivery-QA/. FLAG: announced→opened rules update ships with the train — until deployed, markOpened fails silently server-side and delivery stays announced (harmless; F5 re-offers).
[DONE] F5 shelf catch — the little shelf (RecKeepsakesView, under Profile) now unifies two sources. WRAPPED PARCELS: announced-but-unopened deliveries render as the drawn RecParcelView on cream paper w/ a gift-dashed edge + caption "still wrapped · tap to open" (lowercase, real middot, no dash); a tap presents the SAME F4 RecRevealView by deliveryId (local fullScreenCover, reuse not a fork — dataManager injected through ProfileView). OPENED KEEPSAKES: each opened rec now shows its image thumbnail via RecArtwork's per-type pipeline (itunes/openlibrary/tmdb), paper-only emoji token when no image (never a broken frame), washi mark on kept unchanged. NEW RecShelf.swift (pure, tested): WrappedDelivery model + Entry enum + merge()/visible(). MERGE/SORT RULE: wrapped-and-waiting FIRST (newest announcement first — the catch keeps an unopened gift at the top), then opened keepsakes newest-first. DEDUPE by delivery identity: RichRecStore.openedDeliveryIds (persistent, capped, written at reveal in RecRevealView.recordRevealIfNeeded) drops a just-opened parcel from the wrapped list immediately, covering the fire-and-forget markOpened window before the server flips the doc out of the announced query — no double-show. FILTER: everything (default) shows parcels+keepsakes; kept shows only kept opened keepsakes. Wrapped read = client query deliveries where status=='announced' (same pattern as F3 RecAnnouncementObserver, allowed by the existing no-leak status!='held' rule — ZERO rules/functions changes). Reduce-motion safe (wrapped slip skips appear spring; parcel glow self-gates), dark-mode safe (fixed cream palette, same as F4's fixed dusk), no memory growth (thumbnails lazy in the existing LazyVGrid, URLCache-backed). NEW STRING: "still wrapped · tap to open" ×5 (en + es todavía sin abrir · toca para abrir / ja まだ包んだまま · タップして開けてね / ko 아직 포장된 채예요 · 눌러서 열어봐요 / vi vẫn còn gói kín · chạm để mở); zero existing-string changes. QA hooks: -recShelfWrappedQA (service returns 2 qa- wrapped fixtures, both route through qaDelivery on tap), -recShelfEmptyQA (clears archive, opens empty shelf); both land on the profile tab. DinoTests 426/426 (418 + 8 RecShelfTests: merge/sort, dedupe, kept filter, empty, opened-id persistence, wrapped-QA gating, new-string catalog sentinel). Debug build green. 5 agent-eye-verified screenshots (f5-shelf-mixed, f5-wrapped-parcel-detail, f5-tap-opens-reveal [tapping a parcel opened the real F4 reveal], f5-shelf-dark, f5-shelf-empty) + f5-verdict.md in scratchpad rec-delivery-QA/. crisis hash ba2ebd56… intact; /Users/vikas/Dino + /Users/vikas/Dino-star untouched; nothing pushed/deployed. Carries forward F4's flag: until the announced→opened rules update deploys, markOpened fails silently server-side, but the local openedDeliveryIds set still dedupes on-device.
[DONE] F6 ledger outcomes (79ceb96) — the ANNOUNCEMENT lifecycle now becomes enum-only outcomes so a future distiller can learn KNOCK TIMING. SCHEMA: new kind 'announcement' (itemType 'parcel' = the knock not its contents; moodContext 'none'; action in shown|opened|ignored; daypart), ONE doc per delivery keyed deterministically outcomes/{uid}/entries/ann_{deliveryId} — chosen over a rec-outcome facet because the knock is a DISTINCT event from the reveal and it drops straight into the distiller's existing enum-tuple read shape (kind|itemType|moodContext|daypart|action|followupTrend). WIRING: SHOWN server-side in recDeliverySweep's announce branch, atomic in the announce tx (un-forgeable — clients can never CREATE announcement outcomes; the outcomes create allowlist stays rec/gift); OPENED client-side in RecRevealView.recordRevealIfNeeded via OutcomeLedger.recordAnnouncementOpened — "opened stays where F4 puts it", deduped by the deterministic id + the didRecord single-fire, distinct from F4's kind:rec outcome (knock vs rec are two events → no double-record); IGNORED server-side in a NEW 2nd sweep pass — an announced delivery unopened past 72h (announcedAt+72h) retires status→expired and flips the outcome shown→ignored. JUDGMENT: the spec pointed at "the F2 sweep's 72h expiry path" but that path is HELD→expired (never announced = never knocked); a faithful "knock went unanswered" signal needs announced→expired, so I added it. The ignored-pass tx NEVER clobbers an already-'opened' outcome, so the signal is correct regardless of F4-rule deploy ordering; side effect: announced-unopened deliveries retire from F5's shelf catch after 72h (reasonable symmetry with held-72h). DAYPART set once at SHOWN (daypartFor(now,tz)), preserved through the in-place opened/ignored flips. ENUM LOCKSTEP: client OutcomeLedger gains announcementKind/ItemType/Actions/IdPrefix (deliberately NOT in `kinds`, which mirrors the rules client-create allowlist); server pure module functions/src/outcomes.ts mirrors the same enums + buildAnnouncementOutcome (null on off-enum → writes nothing). RULES: ZERO rules-file changes — SHOWN/IGNORED are admin writes (rules-exempt), OPENED already fits the existing outcomes update rule; +4 rules tests LOCK it (client create denied → shown un-forgeable). New index deliveries(status,announcedAt) for the announced-expiry query. PRIVACY PROOF: built doc's only string values are the fixed enums {announcement,parcel,none,<daypart>,<action>}; content-shaped keys present = NONE; outcomes.test.ts PRIVACY test asserts subset-of-closed-keyset + 8 forbidden content keys absent. Distiller-facing note added to preferences.ts (comment only, no behavior change; consumer deferred). NEW STRINGS: none (data-only). SUITES: DinoTests 426→428 (+2), functions 90→97 (+7), rules 27→31 (+4) — all green. crisis hash ba2ebd56… intact; main 758c14c; /Users/vikas/Dino + /Users/vikas/Dino-star untouched; nothing pushed/deployed. f6-ledger-flow.md + f6-verdict.md in scratchpad rec-delivery-QA/.
[DONE] payload-retention hardening — rec content payload (recDeliveries/{uid}/payloads/{deliveryId}) no longer persists forever. THREE MECHANISMS: (1) DELETE ON OPEN — new onRecDeliveryOpened onDocumentUpdated trigger (recDeliveries/{uid}/deliveries/{deliveryId}); pure guard shouldDeletePayloadOnTransition(before,after) = before!=opened && after==opened (no-ops on announce/reschedule/openedAt-touch/expiry), deletes the sibling payload via admin SDK (idempotent). Client can't delete payloads (rules write:false) so the flip is watched server-side; F4 reads the payload into memory BEFORE markOpened fires, so deleting the server copy post-flip is safe. (2) TTL BACKSTOP — expiresAt = now + PAYLOAD_RETENTION_DAYS(14) at create (payloadExpiresAtMs helper), mirroring outcomes-TTL. 14 not 7: worst-case lifespan HELD_EXPIRY_MS(72h)+ANNOUNCED_EXPIRY_MS(72h)~6d, so ~2x is a true backstop that never races a claimable gift. OWNER CONSOLE STEP: enable Firestore TTL policy on payloads collection group, field expiresAt (code can't set TTL policies). (3) DELETE ON EXPIRY SWEEP — both expire paths (first-pass held->expired + F6 second-pass announced->expired) now tx.delete the sibling payload in the SAME transaction (reads-before-writes respected, idempotent). STALE RE-TAP FIX: fetch now distinguishes purged (payload getDocument succeeds !exists -> Delivery(status,recs:[])) from transient miss (throws -> nil, parcel stays wrapped); RecRevealMachine.shouldDismissToShelf(readable,hasRecs) -> load() dismisses cleanly to shelf on a readable-but-empty delivery instead of leaving an un-openable parcel. VERIFIED: functions node 97->99 green, tsc clean, compiled lib confirmed (trigger export + expiresAt + both tx.delete), rules 31/31 green (rules unchanged), DinoTests +1 test. Rules harness is firestore-only (no functions triggers) — trigger/sweep E2E needs a manual functions-emulator/throwaway run (documented in scratchpad/rec-delivery-QA/payload-retention-fix.md). CrisisResources hash ba2ebd56... intact.

[DONE] F4 reveal polish (feature/rec-delivery, RecRevealView.swift only) — (1) DINO REMOVED from the reveal: deleted the cut-DinoMascot block, the `breathing` @State + its .onAppear breathe animation from private struct RecDuskBackdrop; dropped the now-unused reduceMotion param (call site RecDuskBackdrop()); kept gradient + horizon-glow ellipse + two hills; updated file/struct comments (no strings). Card's Image("jar-dino") untouched. No unused symbols left (w/h + view.reduceMotion still used). (2) BLOOM HAPTIC: HapticManager.success()/import UIKit already present; parcelTapped() now fires HapticManager.shared.success() the instant phase becomes .revealed in BOTH paths (reduce-motion easeInOut path + the unwrap asyncAfter spring path). Once per reveal (existing guard next!=phase + inner guard phase==.unwrapping). Existing light() tap kept. (3) DISCOVERABLE DISMISS: new quiet closeButton (SF Symbol xmark in low-opacity cream circle, top-trailing) added as last ZStack child above the card, action onDismiss; AX label reuses EXISTING "close" catalog key via String(localized:) — no new string. Swipe hardened: .gesture(dismissDrag) -> .simultaneousGesture(dismissDrag) so the revealed card's allowsHitTesting no longer swallows the drag; dismiss works in every phase. VERIFIED: Debug build green; DinoTests 429/429 green (baseline held, no test touched the removed param). Screenshots: f4polish-no-dino.png LIVE (parcel over dusk world, NO dino on hills, close X visible), f4polish-dismissed.png LIVE (home = onDismiss target); f4polish-card.png NOT live (Simulator tap access denied by user, simctl has no tap; card unchanged by this task, verified by build/logic). Verdict: scratchpad/rec-delivery-QA/f4polish-verdict.md. CrisisResources hash ba2ebd56... intact; main untouched; nothing pushed.

[TODO] final: morning report + REC_DELIVERY_DONE

# ── overnight autonomous arc — luna-driven comfort recs (feature/luna-recs, off main @ 36b442a) ──
GOAL: replace the mood-log-time comfort-rec trigger with a nightly LUNA CONCERN-SCORE that
decides when dino should generate + hold comfort recs, and learn delivery cadence from the
announcement outcome ledger.

HARD LIMITS: work ONLY in /Users/vikas/Dino-luna, absolute paths every edit. NEVER touch
/Users/vikas/Dino (main @ 36b442a, owner's checkout) or /Users/vikas/Dino-star (parked).
NEVER modify main directly; NEVER push. CrisisResources.swift + crisis keyword nets UNTOUCHED
(protected sha256 ba2ebd5645e249d0f5148235e6c83de25ebd39b4b653654e60b1a048a763e090).
generateComfortRecs model stays gpt-4.1-mini (owner hard rule; assertRoute enforces).
Secrets.xcconfig + functions/.env copied for build only — gitignored, NEVER commit.

COST RULES:
1. Prefer folding the concern-score into an EXISTING nightly job over a new cron. RECOMMENDED
   HOST: nightlyExpeditionWatch (already reads expeditionSignals + already makes the per-user
   gpt-5.6-luna call). Concern score should be a PURE function of already-bucketed signals →
   ZERO new per-user model call. NOT the distiller (reads outcomes ledger, capped 50/night,
   never sees current mood/sleep/steps signals).
2. Luna nightly model = gpt-5.6-luna (cheap classification tier). Do not widen.
3. Respect existing caps: EXPEDITION_LUNA_NIGHTLY_CAP=2000, COMFORT_REC_DAILY_LIMIT=2.

TASK LIST:
[DONE] 0. archaeology — full inventory in scratchpad/luna-recs-QA/task0-archaeology.md
       (host recommendation, luna model id, reusable-vs-build, cost baseline, schemas,
       F2 trigger at EmotionalWeatherView.swift:203 → generateAndHoldIfMomentIsRight → generateComfortRecs).
[TODO] 1. concern score — deterministic from expeditionSignals buckets, hosted in the watcher (no new model call)
[TODO] 2. trigger math — when concern score → call generateComfortRecs (gates/thresholds)
[2026-07-20] T1+T2 DONE + GREEN. Concern score rides the watcher's existing gpt-5.6-luna call (concern_score field added to its JSON + weighting/personal-baseline instruction in LUNA_WATCHER_PROMPT) — zero added model calls. New pure module functions/src/concernScore.ts: computeConfidence (0.4 mood-only floor → 1.0 all-3-optionals), sanitizeConcernScore, effectiveThreshold=58+(1-conf)*20, decideRecGeneration (caps-first: ≥7d cooldown + ≤4/30d cap from server-owned recDeliveries.announcedAt, THEN confidence-gated threshold — caps independent of score). nightlyExpeditionWatch logs the decision DRY-RUN (does NOT call generateComfortRecs — that's T3). Tests: 108/108 (99 baseline + 9), incl pathological-cap (score=100×60d → 8 deliveries, all ≥7d apart, ≤4/30d) + frequency-shape sim (stable 0.00, typical 2.73, declining 4.00/max 4). tsc clean. Cost: marginal scoring ~$0.0022/user/mo (5–9× under flag). Crisis untouched (no crisis input in the path). QA: t1t2-verdict.md, frequency-sim.md, cost.md. FLAG: score sits after the expedition-gift spacing gates (14d-since-gift can suppress recs) — T3 to decide whether to move it earlier.
[2026-07-20] T3 DONE + GREEN. Rewired the comfort-rec trigger from mood-log-time (client) to the nightly watcher (server-side concern score). PART A (decouple): moved the shared gpt-5.6-luna call AHEAD of the expedition-gift gates so it scores every eligible user (was gated behind 14d-since-gift / sinceLastRec==0to2 → gift-cooldown users never scored). Only the 20h call-frequency guard still gates the call; the two gift gates moved below it as pure predicate expeditionGiftGatesPass and gate the expedition MISSION only — expedition delivery set is IDENTICAL (predicate is the exact complement of the old inline skip; proven exhaustively in tests). Added luna volume bounded by the unchanged 2000-cohort cap (~$0.0017/call, global ceiling ~$100/mo unchanged, typical per-user add ~$0.02-0.03/mo worst case). PART B (wire): extracted generateComfortRecs core into runComfortRecGeneration(uid, ComfortRecInput) — onCall now a thin byte-identical adapter; watcher builds server input via buildWatcherComfortRecInput and calls the SAME hold block (gpt-4.1-mini, deliverAfter/tz/daypart/expiresAt/daily-cap+refund unforked). Server payload: moodTrend/themes/userLocale clean-mapped; timeOfDay='night'; mood/quietTypes/userCountry/excludeTitles → documented safe defaults (FLAG: userCountry='' loses regional resonance + TMDB poster; excludeTitles=[] may repeat a title — no server source for either). PART C (remove client trigger): deleted the EmotionalWeatherView else-branch generateAndHoldIfMomentIsRight Task; mood-log still writes the entry (saveMood) + updates expeditionSignals (.task syncIfNeeded) — only generation trigger gone. generateAndHold + ComfortRecCoordinator.fetchIfMomentIsRight now cleanly unreferenced (left defined + documented); GentleRecService untouched; onCall stays deployed. Suites: functions 113/113 (108+5), tsc clean; DinoTests 429/429 (baseline, no test asserted the trigger). Rules unchanged (31). Crisis hash ba2ebd56... intact; crisisNet.ts untouched; main 36b442a; Dino + Dino-star untouched; nothing pushed/deployed. Verdict: scratchpad/luna-recs-QA/t3-verdict.md. FLAG: a one-time silent git-restore reverted index.ts+concernScore.ts mid-edit (re-applied + re-verified) — possible parallel job in the worktree.
[TODO] 3. (done above) rewire trigger — replace EmotionalWeatherView:203 mood-log trigger with the nightly path
[2026-07-20] T4 DONE + GREEN. Ledger-based personalized cadence: a user's own announcement (knock)
       engagement nudges their effective rec threshold — consistent OPENS lower the bar (slightly more
       recs), consistent IGNORING raises it (fewer). PURE deterministic ratio math (NO new job, NO new
       model call, $0 added — cost rule 1): folded compute into the EXISTING nightlyPreferenceDistill and
       consume into the EXISTING nightlyExpeditionWatch. SIGNAL: F6 announcement outcomes (kind announcement,
       itemType parcel, action shown|opened|ignored) over the most-recent 30 knocks; open-rate =
       opened/(opened+ignored), 'shown' excluded from the ratio. MAPPING (symmetric, clamped int [-6,+6]):
       r≥0.80→-6, 0.60-0.80→-3, 0.40-0.60→0 (neutral), 0.20-0.40→+3, r<0.20→+6; <4 resolved knocks → exactly 0.
       STORED: recThresholdAdjustment on prefs/{uid} (server-only write; enum/number, never content). Bounds
       live in concernScore + imported by preferences (lockstep). RULES UNCHANGED (31) — prefs is allow write:false,
       no key allowlist to extend. WATCHER reads it from the prefs doc it ALREADY reads (no new Firestore read),
       sanitizes→clamps, passes into decideRecGeneration; effectiveThreshold = 58 + (1-conf)*20 + adj, clamped
       [50,80]. CAPS STILL HARD: extended pathological test — most-engaged user (adj -6) + score=100 every night
       for 60d → STILL ≤1/7d and ≤4/30d (caps checked before score, independent of both score AND adjustment).
       Suite 125/125 (113 baseline + 12 new), tsc clean. No Swift touched. Crisis untouched (no crisis input in
       the path). QA: t4-verdict.md. TASKS 0-4 ALL GREEN → ready for final report.
[TODO] final: morning report + LUNA_RECS_DONE
